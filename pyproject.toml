[project]
name = "newsgen"
version = "0.1.0"
description = "Generate today's news webpage using Claude AI"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "typer>=0.12.0",
    "rich>=13.0.0",
]
